(include_subdirs unqualified)

(library
  (name dwarf_high)
  (wrapped true)
  (ocamlopt_flags
   (:standard
    (:include %{project_root}/ocamlopt_flags.sexp)
    -O3))
  (libraries dwarf_low asm_targets)
)
